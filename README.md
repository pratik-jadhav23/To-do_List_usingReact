# React + Vite
Download this project as zip
then open this project in vscode and type "npm i" in terminal on downloaded directory
then To run this project use "npm run dev" cmd in present working directory 
